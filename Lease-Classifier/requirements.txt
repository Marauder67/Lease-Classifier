# Core service
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9

# Extraction (LLM) — Anthropic Claude is the default provider
anthropic>=0.34

# Testing
pytest>=8.0
httpx>=0.27
